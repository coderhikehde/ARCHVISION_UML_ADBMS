{
  "name": "@archvision/cli",
  "version": "0.1.0",
  "description": "ArchVision AI CLI — validate architecture and export diagrams in CI",
  "bin": {
    "archvision": "./dist/cli.js"
  },
  "type": "module",
  "scripts": {
    "build": "tsc --project tsconfig.json",
    "test": "node --test dist/*.test.js"
  },
  "dependencies": {
    "commander": "^12.0.0"
  },
  "devDependencies": {
    "@types/node": "^20",
    "typescript": "^5"
  }
}
